package com.bats.lite.repository;

import com.bats.lite.entity.QeueEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface QeueRepository extends JpaRepository<QeueEntity, Long> {
}
